import 'package:flutter_test/flutter_test.dart';

void main() {
  test('check if core files exist', () {
    // This test will help us see what imports actually work
    expect(true, isTrue); // Always passes - we're just checking imports
  });
}